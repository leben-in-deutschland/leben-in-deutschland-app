# Features

No project feature specification was created during PULSE bootstrap because
the bootstrap does not change product behavior.

Copy `_template.md` when a requested user or system behavior benefits from a
durable specification. Use `NNN-short-title.md` and one of these statuses:
Draft, Ready, In Progress, Done, Paused.
