# Plans

No persistent project plan was created during PULSE bootstrap. The bootstrap
rollback checklist is session-scoped because it protects only this
installation.

Copy `_template.md` for non-trivial implementation, migration, integration, or
recovery work. Every plan must include a baseline, trigger, narrow reversal,
state-safety notes, recovery checks, and measurable acceptance criteria.
